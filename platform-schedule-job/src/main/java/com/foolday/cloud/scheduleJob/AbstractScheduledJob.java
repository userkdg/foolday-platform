package com.foolday.cloud.scheduleJob;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public abstract class AbstractScheduledJob {
    protected final static Logger logger = LoggerFactory.getLogger(AbstractScheduledJob.class);
}
